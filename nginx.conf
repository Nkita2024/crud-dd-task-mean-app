events {}

http {
    server {
        listen 80;

        # Frontend served at /
        location / {
            proxy_pass http://frontend:80;
        }

        # Backend API served at /api/
        location /api/ {
            proxy_pass http://backend:8080/api/;
        }
    }
}
